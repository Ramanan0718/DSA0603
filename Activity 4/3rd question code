# Purchase Amount
purchase <- c(250,300,320,350,380,450,520,650,1800,5200)

# Histogram
hist(purchase,
     probability=TRUE,
     main="Histogram of Purchase Amount",
     xlab="Purchase Amount",
     col="lightblue")

# Density Curve
lines(density(purchase), col="red", lwd=2)

grid()
